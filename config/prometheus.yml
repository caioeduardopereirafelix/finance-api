global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "api-finance-job"
    metrics_path: "/actuator/prometheus"

    static_configs:
      - targets:
          - "api:9091"

        labels:
          application: "api-finance"